# === Required ===

# PostgreSQL connection string
# On DigitalOcean App Platform this is auto-injected via ${db.DATABASE_URL}
DATABASE_URL="postgresql://user:password@host:25060/db?sslmode=require"

# === LLM Configuration ===

# "dry_run" (default) returns placeholder text without calling any API
# "real" calls OpenAI/Anthropic APIs (requires API key below)
LLM_MODE="dry_run"

# API keys — set whichever provider(s) you use
# OPENAI_API_KEY="sk-..."
# ANTHROPIC_API_KEY="sk-ant-..."

# Default provider when model name is ambiguous: "openai" or "anthropic"
# LLM_PROVIDER_DEFAULT="openai"

# === Spend Controls (recommended for real mode) ===

# Maximum USD spend per distillation run
# LLM_MAX_USD_PER_RUN=5.00

# Maximum USD spend per calendar day
# LLM_MAX_USD_PER_DAY=20.00

# Minimum delay between LLM API calls in milliseconds (default 250)
# LLM_MIN_DELAY_MS=250
